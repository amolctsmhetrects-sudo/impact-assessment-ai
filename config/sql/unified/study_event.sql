SELECT
    study_event_id,
    study_id,
    event_code,
    event_name,
    event_category,
    event_type,
    planned_date,
    actual_date,
    event_status,
    event_description,
    event_owner,
    created_date,
    modified_date
FROM landing.study_event
